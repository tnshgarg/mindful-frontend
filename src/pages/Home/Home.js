import React from "react";
import Button from "../../components/Button/Button";
var Home = function () {
    return (React.createElement(Button, { variant: "box", wide: true, title: "Get Started", navUrl: "/pre-registration" }));
};
export default Home;
